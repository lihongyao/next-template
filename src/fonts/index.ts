import { Albert_Sans, Alumni_Sans, Inter, Tilt_Warp } from 'next/font/google';

export const alumniSans = Alumni_Sans({
  subsets: ['latin'],
  display: 'swap',
  preload: false,
});

export const inter = Inter({
  subsets: ['latin'],
  display: 'swap',
  preload: false,
});
export const albertSans = Albert_Sans({
  subsets: ['latin'],
  display: 'swap',
  preload: false,
});

export const tiltWarp = Tilt_Warp({
  subsets: ['latin'],
  display: 'swap',
  preload: false,
});
